import 'package:flutter/material.dart';

class DressWidget extends StatelessWidget {
  const DressWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
